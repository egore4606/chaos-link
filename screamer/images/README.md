Положите сюда PNG, JPG/JPEG, BMP, GIF, TIFF или ICO. При запуске скримера выбирается одна случайная картинка; GIF воспроизводится как анимация.

Если у картинки и звука одинаковое имя до расширения, они запускаются вместе. Например: `monster.gif` + `monster.mp3`.
